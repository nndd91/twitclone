$( document ).on('turbolinks:load', function() {
  $('buttonID').click(function () {
      $('theme_css').html("<%= j stylesheet_link_tag 'red', media: 'all', 'data-turbolinks-track': 'reload' %>");
  });
});
